{
  "name": "store_memory",
  "parameters": {
    "type": "object",
    "properties": {
      "subject": {"type": "string"},
      "predicate": {
        "type": "string",
        "enum": [
          "believed", "assumed", "made", "saw", "said to",
          "planned", "requested", "met with", "destroyed", "modified"
        ]
      },
      "object": {"type": "string"},
      "location": {"type": "string"},
      "timestamp": {"type": "integer"}
    },
    "required": ["subject", "predicate", "object"]
  }
}